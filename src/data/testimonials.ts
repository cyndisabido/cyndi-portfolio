export type Testimonial = {
  id: string;
  quote: string;
  name: string;
  role: string;
  company: string;
};

/** Reserved for future use — not rendered on the site yet. */
export const testimonials: Testimonial[] = [];
